import styled from 'styled-components';
import img from '../../assets/corner-img.png';

export const IhorObject = styled.div`
  position: absolute;
  bottom: 90px;
  left: 45px;
  width: 300px;
  height: 300px;
  background: url(${img});
  background-size: contain;
`;
